# Lấy 3 giá trị này ở Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Khóa service_role — CHỈ dùng phía máy chủ, không bao giờ để lọt ra trình duyệt.
# Dùng cho luồng người đề nghị nộp hồ sơ mà không cần đăng nhập.
SUPABASE_SERVICE_ROLE_KEY=
